{
  "character_extraction": {
    "chunk_size_tokens": 32000,
    "_temperature_note": "0 because reading a cast is not a writing task. At 0.3, 22 of the 90 characters in Pride and Prejudice are mentioned exactly once and each was a coin flip: successive runs of the same file found 89, 91, 88, 86, 85, 77, 80 and 82 people.",
    "temperature": 0.0,
    "similarity_threshold": 0.8,
    "deduplication_similarity_threshold": 80
  },
  "transformation": {
    "paragraphs_per_batch": 100,
    "temperature": 0.3,
    "max_retries": 3,
    "target_token_utilization": 0.66,
    "max_tokens_per_request": 120000
  },
  "services": {
    "parser": {
      "class": "src.services.parser_service.ParserService",
      "config": {
        "cache_enabled": true
      }
    },
    "character": {
      "class": "src.services.character_service.CharacterService",
      "config": {
        "cache_enabled": true,
        "max_concurrent": 5
      },
      "dependencies": {
        "provider": "llm_provider"
      }
    },
    "transform": {
      "class": "src.services.transform_service.TransformService",
      "config": {
        "cache_enabled": true,
        "max_concurrent": 5
      },
      "dependencies": {
        "provider": "llm_provider",
        "character_service": "character"
      }
    }
  },
  "logging": {
    "level": "INFO"
  }
}